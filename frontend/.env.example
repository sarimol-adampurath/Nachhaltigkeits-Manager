# Local development default
VITE_API_BASE_URL=http://localhost:8000/api

# For single-host deployment you can set this to /api or leave it unset
# because production default already falls back to /api.
# VITE_API_BASE_URL=/api
